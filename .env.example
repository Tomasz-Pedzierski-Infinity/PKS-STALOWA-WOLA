# Google Gemini API — required for the PEKAESIK chatbot (free tier, no card)
# Obtain a key at https://aistudio.google.com/app/apikey
# Locally: copy to .env.local. On Vercel: set via dashboard or `vercel env`.
GOOGLE_GENERATIVE_AI_API_KEY=
